apiVersion: v1
kind: Service
metadata:
  name: mcp-checkuptime
  namespace: mcp
  labels:
    app.kubernetes.io/name: mcp-checkuptime
    app.kubernetes.io/instance: mcp-checkuptime
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 9000
  selector:
    app.kubernetes.io/name: mcp-checkuptime
    app.kubernetes.io/instance: mcp-checkuptime
