apiVersion: v1
kind: Secret
metadata:
  name: mcp-bearer-token
  namespace: mcp
  labels:
    app.kubernetes.io/name: mcp-checkuptime
    app.kubernetes.io/component: authentication
type: Opaque
stringData:
  # Replace with your actual bearer token
  # Or create the secret via kubectl command instead
  token: ""  # Leave empty if creating via kubectl

